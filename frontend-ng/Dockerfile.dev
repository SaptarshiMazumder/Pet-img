# Dev: run ng serve with hot reload (no production build)
FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci

# Source is mounted via volume; default command runs dev server
EXPOSE 4200

CMD ["npm", "start", "--", "--host", "0.0.0.0", "--poll", "2000"]
